export interface AppConfig{
    MONGODB_URI: string, 
    SESSION_SECRET: string,
    PORT: Number,
    DB_HOST: String,
    DB_PORT: Number,
    DB_USERNAME: String,
    DB_PASSWORD: String,
    DB_NAME: String,
}